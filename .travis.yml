language: generic

sudo: required
services:
 - docker

before_install:
 - docker build -t  yigityilmaz/docker-react -f Dockerfile.dev .

script:
 - docker run -e CI=true yigityilmaz/docker-react npm run test

deploy:
 provıder: elasticbeanstalk
 region: "us-east-2"
 app: "docker-react"
 env: "DockerReact-env"
 bucket_name: "elasticbeanstalk-us-east-2-710432783481"
 bucket_path: "docker-react"
 on:
  branch: master